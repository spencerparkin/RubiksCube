// Solver.h

//==================================================================================================
class Solver
{
public:

	Solver( void );
	~Solver( void );

	bool MakeMove( RubiksCube::Rotation& rotation );
};

// Solver.h